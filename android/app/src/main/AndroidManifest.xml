<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 📍 صلاحيات الموقع الجغرافي والإنترنت -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="tourist_services1"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- 🔑 مفتاح Google Maps API -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBU0BZeORfj2mNcARrUc4IfYZUm0zQggiA" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- 🔗 السماح بفتح التطبيقات الخارجية (Google Maps, Phone Calls, Web) عبر url_launcher -->
    <queries>
        <!-- لفتح خرائط Google Maps والروابط -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <!-- لإجراء المكالمات الهاتفية المباشرة -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <!-- لمعالجة النصوص -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>